import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 12,
  },

  card: {
    padding: 16,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: '#eee',
    backgroundColor: '#fff',

    marginBottom: 12,
  },
  list: {
    padding: 12,
  },
  cartContainer: {
    padding: 12,
    borderBottomWidth: 1,
    borderColor: '#eee',
  },
});
